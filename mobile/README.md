# Mobile Application Placeholder

This folder is reserved for Phase 2 of the NexToken platform: The native Android application (and potential future iOS deployment).

The web application has been designed with responsive layouts and PWA capabilities. The mobile app will either wrap the DApp using a specialized WebView component or utilize a full React Native / Expo rewrite pulling from the same backend API layer.

**Development Note:** Do not write web code in this directory.
