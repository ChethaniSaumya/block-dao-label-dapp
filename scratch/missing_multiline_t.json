[
  "",
  "content-type",
  "The BDF token is a BEP-20 asset issued on the BSC (Binance Smart Chain) network. Block Label leverages BSC's high throughput, low fees, and proven security to bring entertainment IP and the fan economy on-chain — contributing to the expansion of real-world use cases within the BSC ecosystem.\",\n          )}\n        </p>\n        <div className=\"mt-6 grid sm:grid-cols-3 gap-4\">\n          {bscPoints.map((b) => (\n            <div key={b.title} className=\"p-4 rounded-lg tile\">\n              <b.icon className=\"w-4.5 h-4.5 text-gold-lift\" />\n              <div className=\"mt-2.5 text-sm font-semibold\">{t(b.title)}</div>\n              <p className=\"mt-1.5 text-xs text-muted-foreground leading-relaxed\">\n                {t(b.body)}\n              </p>\n            </div>\n          ))}\n        </div>\n        <div className=\"mt-6 flex gap-2.5 text-xs text-muted-foreground p-3 rounded-md bg-secondary\">\n          <Info className=\"w-3.5 h-3.5 shrink-0 mt-0.5\" />\n          <p>\n            {t(\n              \"This is an independent project built on the BSC (Binance Smart Chain) network. While we currently have no capital or equity ties with Binance or its affiliates, we are actively pursuing strategic collaborations and partnerships with projects across the BSC ecosystem going forward.\",\n            )}\n          </p>\n        </div>\n      </Card>\n\n      {/* Partners & Ecosystem — About.pdf */}\n      <div className=\"mt-12 text-center\">\n        <div className=\"eyebrow\">{t(\"Partners & Ecosystem",
  "Progress assumes sequential issuance; serials issued out of order are not reflected in the bars.\",          )}        </p>      </Card>      {/* ── Issuance ──────────────────────────────────────────────────── */}      <div className=\"grid lg:grid-cols-2 gap-6 mt-6\">        <Card className={isMinter ? \"border-gold/30\" : \"\"}>          <div className=\"flex items-center justify-between mb-1\">            <h2 className=\"font-semibold\">{t(\"Issue to a Buyer",
  "A next-generation entertainment and technology company combining AI and blockchain — building a healthy ecosystem where technology and art work together.\",              )}            </p>            <div className=\"mt-5 space-y-1 text-sm\">              <div className=\"flex items-center gap-2\">                <span className=\"text-muted-foreground\">{t(\"Enquiries",
  "Use when the buyer must receive the number printed on their physical certificate.\",              )}            </p>            <div className=\"grid grid-cols-3 gap-2\">              <div className=\"col-span-2\">                <Field                  label={t(\"Buyer BSC wallet",
  "Pausing halts all issuance AND all holder transfers. Use only for a live security incident.\",              )}            </p>            <OutlineButton              className=\"w-full px-4 py-2\"              disabled={!isAdmin || busy}              onClick={() => {                if (!confirm(t(\"Confirm pause state change for the whole collection?",
  "{{BRAND}} ({{SYMBOL}}) is the native utility and governance token of the Block Label Foundation ecosystem on BNB Chain — a fixed 10-billion supply, community-first distribution, and a rules-based buyback & burn.\",                )}              </p>              <div className=\"mt-10 flex flex-wrap gap-4\">                {/* Launch App — opens wallet modal if not connected, else goes to dashboard */}                <ConnectButton.Custom>                  {({                    account,                    chain,                    openAccountModal,                    openChainModal,                    openConnectModal,                    mounted,                  }) => {                    const connected = mounted && account && chain;                    return (                      <GoldButton                        onClick={                          !connected                            ? openConnectModal                            : chain.unsupported                              ? openChainModal                              : () => (window.location.href = \"/dashboard",
  "/api/admin/bulk-airdrop\", {\n    method: \"POST\",\n    body: JSON.stringify({ recipients }),\n  });\n}\n\nexport async function getAdminLogs(): Promise<AdminLog[]> {\n  return request(\"/api/admin/logs",
  "Your Block Label Creator DAO Certificates. Each certificate carries a unique serial number, an allocated {{SYMBOL}} airdrop, and access to the IP wholesale market.\",          )}        </p>      </div>      {/* Token-gating status — Issuance Plan §12.4 */}      <Card className={holds ? \"border-gold/40\" : \"\"}>        <div className=\"flex items-start gap-4\">          {holds ? (            <ShieldCheck className=\"w-8 h-8 text-gold shrink-0\" />          ) : (            <Lock className=\"w-8 h-8 text-muted-foreground shrink-0\" />          )}          <div className=\"flex-1\">            <h2 className=\"font-semibold\">              {holds ? t(\"Wholesale access granted",
  "Creator DAO Certificate",
  "Lowering the supply cap is permanent — it can never be raised again. Continue?\",\n                    ),\n                  )\n                )\n                  return;\n                send(t(\"Supply cap reduction",
  "Verified participants claim {{SYMBOL}} through an open Merkle distributor — pull-based, on-chain, no batch transfers.\",\n        )}\n      />\n\n      <div className=\"grid md:grid-cols-2 gap-8 mt-8\">\n        <div className=\"space-y-6\">\n          {/* Claim card */}\n          <Card ornament>\n            <div className=\"eyebrow mb-5\">{t(\"Your Claim",
  "Verified participants claim {{SYMBOL}} through an open Merkle distributor — pull-based, on-chain, no batch transfers.\",        )}      />      <div className=\"grid md:grid-cols-2 gap-8 mt-8\">        <div className=\"space-y-6\">          {/* Claim card */}          <Card ornament>            <div className=\"eyebrow mb-5\">{t(\"Your Claim",
  "{{BRAND}} ({{SYMBOL}}) is the native utility and governance token of the ecosystem, built on BNB Chain (BEP-20). The total supply is fixed at 10,000,000,000 {{SYMBOL}} with no minting capability, and 50% is allocated to the community through the Fan DAO and Creator DAO. Distribution is fully on-chain, transparent, and independently verifiable.\",\n          )}\n        </p>\n        <p className=\"mt-4 text-sm text-muted-foreground\">\n          {t(\n            \"Rather than an unconditional distribution, {{SYMBOL}} is claimed by verified ecosystem participants who meet corporate DAO and staking conditions. A rules-based buyback & burn — funded by real IP value growth — continuously reduces supply, and the team allocation is locked until the burn milestone is reached.\",\n          )}\n        </p>\n      </Card>\n\n      {/* Four pillars — About.pdf */}\n      <p className=\"mt-8 text-sm italic text-center text-muted-foreground max-w-xl mx-auto\">\n        {t(\n          \"“Building a healthy entertainment ecosystem where technology and art harmonize.”\",\n        )}\n      </p>\n      <div className=\"mt-6 grid sm:grid-cols-2 gap-5\">\n        {pillars.map((p) => (\n          <Card key={p.title}>\n            <div className=\"flex items-center gap-3\">\n              <div className=\"w-9 h-9 rounded-md bg-secondary flex items-center justify-center shrink-0\">\n                <p.icon className=\"w-4.5 h-4.5 text-gold-lift\" />\n              </div>\n              <h3 className=\"font-semibold text-base\">{t(p.title)}</h3>\n            </div>\n            <p className=\"mt-3 text-sm text-muted-foreground leading-relaxed\">\n              {t(p.body)}\n            </p>\n            <hr className=\"rule-gold my-4\" />\n            <ul className=\"space-y-1.5\">\n              {p.checklist.map((c) => (\n                <li key={c} className=\"flex items-center gap-2 text-sm\">\n                  <Check className=\"w-3.5 h-3.5 text-gold shrink-0\" />\n                  <span>{t(c)}</span>\n                </li>\n              ))}\n            </ul>\n          </Card>\n        ))}\n      </div>\n\n      {/* BSC ecosystem contribution — About.pdf */}\n      <Card className=\"mt-8\" ornament>\n        <div className=\"eyebrow mb-2\">{t(\"BSC Ecosystem Contribution",
  "Please switch your wallet to BNB Smart Chain to interact with {{BRAND}}.\",            )}          </p>          <GoldButton            onClick={() =>              switchChain && switchChain({ chainId: REQUIRED_CHAIN_ID })            }            disabled={!switchChain}            className=\"w-full justify-center\"          >            {t(\"Switch Network",
  "Deploy the collection, then set VITE_CERTIFICATE_NFT_ADDRESS in the environment to enable this console.\",\n            )}\n          </p>\n          <div className=\"mt-6 text-left text-[11px] font-mono bg-secondary rounded-md p-3 space-y-1\">\n            <div>cd contracts</div>\n            <div>npm run nft:metadata &amp;&amp; npm run nft:pin</div>\n            <div>npm run nft:deploy:testnet</div>\n          </div>\n        </Card>\n      </Section>\n    );\n  }\n\n  if (!isMinter && !isAdmin) {\n    return (\n      <Section className=\"py-20\">\n        <Card className=\"max-w-lg mx-auto text-center\">\n          <ShieldAlert className=\"w-12 h-12 mx-auto text-destructive mb-4\" />\n          <h2 className=\"text-xl font-semibold mb-2\">{t(\"Access Denied",
  "Join the Block Label ecosystem and receive {{SYMBOL}} airdrops automatically through Link2E.\",              )}            </p>            <div className=\"mt-10 flex justify-center gap-3 flex-wrap\">              <WalletButton />              <Link to=\"/dashboard\">                <OutlineButton>{t(\"View Dashboard",
  "Progress assumes sequential issuance; serials issued out of order are not reflected in the bars.\",\n          )}\n        </p>\n      </Card>\n\n      {/* ── Issuance ──────────────────────────────────────────────────── */}\n      <div className=\"grid lg:grid-cols-2 gap-6 mt-6\">\n        <Card className={isMinter ? \"border-gold/30\" : \"\"}>\n          <div className=\"flex items-center justify-between mb-1\">\n            <h2 className=\"font-semibold\">{t(\"Issue to a Buyer",
  "Join the Block Label ecosystem and receive {{SYMBOL}} airdrops automatically through Link2E.\",\n              )}\n            </p>\n            <div className=\"mt-10 flex justify-center gap-3 flex-wrap\">\n\n              <WalletButton />\n              <Link to=\"/dashboard\">\n                <OutlineButton>{t(\"View Dashboard",
  "A next-generation entertainment and technology company combining AI and blockchain — building a healthy ecosystem where technology and art work together.\",\n              )}\n            </p>\n            <div className=\"mt-5 space-y-1 text-sm\">\n              <div className=\"flex items-center gap-2\">\n                <span className=\"text-muted-foreground\">{t(\"Enquiries",
  "{{BRAND}} ({{SYMBOL}}) is the native utility and governance token of the Block Label Foundation ecosystem on BNB Chain — a fixed 10-billion supply, community-first distribution, and a rules-based buyback & burn.\",\n                )}\n              </p>\n              <div className=\"mt-10 flex flex-wrap gap-4\">\n                {/* Launch App — opens wallet modal if not connected, else goes to dashboard */}\n                <ConnectButton.Custom>\n                  {({\n                    account,\n                    chain,\n                    openAccountModal,\n                    openChainModal,\n                    openConnectModal,\n                    mounted,\n                  }) => {\n                    const connected = mounted && account && chain;\n                    return (\n                      <GoldButton\n                        onClick={\n                          !connected\n                            ? openConnectModal\n                            : chain.unsupported\n                              ? openChainModal\n                              : () => (window.location.href = \"/dashboard",
  "Invite wallets to the {{BRAND}} ecosystem and earn rewards.\",\n        )}\n      />\n\n      <Card>\n        <label className=\"text-sm text-muted-foreground\">\n          {t(\"Your Referral Link",
  "Mint a run of consecutive serials to one wallet (e.g. the Safe) to hold as stock.\",\n              )}\n            </p>\n            <div className=\"grid grid-cols-3 gap-2\">\n              <div className=\"col-span-2\">\n                <Field\n                  label={t(\"Destination wallet",
  "/api/transactions/record\", {\n    method: \"POST\",\n    body: JSON.stringify(tx),\n  });\n}\n\n// ─── Airdrop ───\n\nexport interface AirdropStatus {\n  eligible: boolean;\n  webKeyBalance: string;\n  webKeyBalanceFormatted: string;\n  threshold: string;\n  thresholdFormatted: string;\n  rewardAmount: number;\n  /** Source of eligibility: \"BDFStaking\" (540-day lock) or legacy Block Label credit. */\n  source?: string;\n  /** Raw BDF staked (when using the staking contract). */\n  stakedWkeyDao2?: string;\n  /** Global stake order (drives airdrop round / early-bird tier). */\n  stakeSequence?: number;\n  /** Unix seconds when the 540-day lock ends. */\n  unlockTime?: number;\n  /** Seconds remaining on the 540-day lock. */\n  remainingLockSeconds?: string;\n}\n\nexport async function getAirdropStatus(\n  address: string,\n): Promise<AirdropStatus> {\n  return request(`/api/airdrop/status/${address}`);\n}\n\n// ─── Airdrop Merkle claim (BDFAirdropDistributor) ───\n\nexport interface AirdropProof {\n  round: number;\n  index: number;\n  account: string;\n  amount: string; // wei\n  proof: `0x${string}`[];\n}\n\n/** Fetch a wallet's Merkle claim for a round (404 if not included). */\nexport async function getAirdropProof(\n  round: number,\n  address: string,\n): Promise<AirdropProof> {\n  return request(`/api/airdrop/proof/${round}/${address}`);\n}\n\nexport interface AirdropRoundMeta {\n  round: number;\n  root: string;\n  count: number;\n}\n\nexport async function getAirdropRound(round: number): Promise<AirdropRoundMeta> {\n  return request(`/api/airdrop/round/${round}`);\n}\n\n/** Admin: build a round's Merkle tree; returns the root to set on-chain. */\nexport async function adminBuildRound(\n  round: number,\n  claims: { address: string; amount: string | number }[],\n): Promise<AirdropRoundMeta> {\n  return request(\"/api/admin/airdrop/build-round\", {\n    method: \"POST\",\n    body: JSON.stringify({ round, claims }),\n  });\n}\n\n// ─── Referrals ───\n\nexport interface Referral {\n  id: string;\n  referrer_address: string;\n  referred_address: string;\n  join_date: string;\n  reward_amount: number;\n  status: string;\n}\n\nexport interface ReferralData {\n  referrals: Referral[];\n  stats: {\n    totalReferrals: number;\n    totalRewards: number;\n    pendingRewards: number;\n    stakedBlockLabel?: string;\n  };\n}\n\nexport async function getReferrals(address: string): Promise<ReferralData> {\n  return request(`/api/referral/${address}`);\n}\n\n\n// ─── DAO ───\n\nexport interface DaoAccount {\n  address: string;\n  role: string;\n  tokens_held: number;\n  votes_cast: number;\n  rewards_earned: number;\n}\n\nexport interface DaoProposal {\n  id: string;\n  proposal_id: string;\n  title: string;\n  status: string;\n  voted_at: string;\n}\n\nexport interface DaoParticipation {\n  id: string;\n  type: string;\n  description: string;\n  date: string;\n  reward: number;\n}\n\nexport interface DaoData {\n  account: DaoAccount;\n  proposals: DaoProposal[];\n  participation: DaoParticipation[];\n}\n\nexport async function getDaoAccount(address: string): Promise<DaoData> {\n  return request(`/api/dao/account/${address}`);\n}\n\nexport async function updateDaoAccount(\n  address: string,\n  updates: Partial<DaoAccount>,\n): Promise<DaoAccount> {\n  return request(`/api/dao/account/${address}`, {\n    method: \"PUT\",\n    body: JSON.stringify(updates),\n  });\n}\n\n// ─── Admin ───\n\nexport async function adminAirdrop(\n  to: string,\n  amount: string,\n): Promise<{ txHash: string }> {\n  return request(\"/api/admin/airdrop\", {\n    method: \"POST\",\n    body: JSON.stringify({ to, amount }),\n  });\n}\n\nexport interface AdminLog {\n  id: string;\n  action: string;\n  performed_by: string;\n  target_address: string;\n  amount: string;\n  tx_hash: string;\n  timestamp: string;\n}\n\nexport interface UserEligibility {\n  address: string;\n  name: string | null;\n  webKeyBalance: number;\n  eligible: boolean;\n  rewardAmount: number;\n}\n\nexport async function getEligibilityList(): Promise<UserEligibility[]> {\n  return request(\"/api/admin/users-eligibility",
  "We never store your private keys. All transactions are signed locally.\",\n          )}\n        </p>\n\n        <p className=\"mt-4 text-center text-xs\">\n          <Link\n            to=\"/dashboard\"\n            className=\"text-[var(--gold-lift)] hover:underline\"\n          >\n            {t(\"Skip to demo dashboard →",
  "/api/user/profile\", {    method: \"PUT\",    body: JSON.stringify(updates),  });}// ─── Token ───export interface TokenBalance {  address: string;  balance: string;  symbol: string;}export async function getTokenBalance(address: string): Promise<TokenBalance> {  return request(`/api/token/balance/${address}`);}export interface TokenStats {  totalSupply: string;  symbol: string;  chain: string;  contractAddress: string;}export async function getTokenStats(): Promise<TokenStats> {  return request(\"/api/token/stats",
  "Mint a run of consecutive serials to one wallet (e.g. the Safe) to hold as stock.\",              )}            </p>            <div className=\"grid grid-cols-3 gap-2\">              <div className=\"col-span-2\">                <Field                  label={t(\"Destination wallet",
  "50% of supply is community-allocated (Fan DAO + Creator DAO). Team tokens are locked in a Team Vault and released only when the 50% burn milestone is reached, with pro-rata burn applied.\",        )}      </p>      {/* Core Values */}      <h2 className=\"mt-12 mb-4 text-xl font-semibold\">{t(\"Core Values",
  "Serial ranges must ascend and must not overlap. Gaps are allowed — a serial no round covers simply has no allocation.\",            )}          </p>          <div className=\"space-y-2\">            {roundDraft.map((r, i) => (              <div key={i} className=\"grid grid-cols-12 gap-2 items-end\">                <div className=\"col-span-1 text-xs text-muted-foreground pb-2.5\">R{i + 1}</div>                <div className=\"col-span-3\">                  <Field                    label={i === 0 ? t(\"From serial",
  "50% of supply is community-allocated (Fan DAO + Creator DAO). Team tokens are locked in a Team Vault and released only when the 50% burn milestone is reached, with pro-rata burn applied.\",\n        )}\n      </p>\n\n      {/* Core Values */}\n      <h2 className=\"mt-12 mb-4 text-xl font-semibold\">{t(\"Core Values",
  "Your Block Label Creator DAO Certificates. Each certificate carries a unique serial number, an allocated {{SYMBOL}} airdrop, and access to the IP wholesale market.\",\n          )}\n        </p>\n      </div>\n\n      {/* Token-gating status — Issuance Plan §12.4 */}\n      <Card className={holds ? \"border-gold/40\" : \"\"}>\n        <div className=\"flex items-start gap-4\">\n          {holds ? (\n            <ShieldCheck className=\"w-8 h-8 text-gold shrink-0\" />\n          ) : (\n            <Lock className=\"w-8 h-8 text-muted-foreground shrink-0\" />\n          )}\n          <div className=\"flex-1\">\n            <h2 className=\"font-semibold\">\n              {holds ? t(\"Wholesale access granted",
  "Deploy the collection, then set VITE_CERTIFICATE_NFT_ADDRESS in the environment to enable this console.\",            )}          </p>          <div className=\"mt-6 text-left text-[11px] font-mono bg-secondary rounded-md p-3 space-y-1\">            <div>cd contracts</div>            <div>npm run nft:metadata &amp;&amp; npm run nft:pin</div>            <div>npm run nft:deploy:testnet</div>          </div>        </Card>      </Section>    );  }  if (!isMinter && !isAdmin) {    return (      <Section className=\"py-20\">        <Card className=\"max-w-lg mx-auto text-center\">          <ShieldAlert className=\"w-12 h-12 mx-auto text-destructive mb-4\" />          <h2 className=\"text-xl font-semibold mb-2\">{t(\"Access Denied",
  "Serial ranges must ascend and must not overlap. Gaps are allowed — a serial no round covers simply has no allocation.\",\n            )}\n          </p>\n\n          <div className=\"space-y-2\">\n            {roundDraft.map((r, i) => (\n              <div key={i} className=\"grid grid-cols-12 gap-2 items-end\">\n                <div className=\"col-span-1 text-xs text-muted-foreground pb-2.5\">R{i + 1}</div>\n                <div className=\"col-span-3\">\n                  <Field\n                    label={i === 0 ? t(\"From serial",
  "Ownership verified on-chain. You can browse and purchase IP wholesale products.\",                  )                : t(                    \"This wallet holds no Creator DAO Certificate. Certificates are issued by the Foundation once your order is confirmed.\",                  )}            </p>          </div>          {holds && (            <div className=\"text-right shrink-0\">              <div className=\"text-xs text-muted-foreground\">{t(\"Membership tier",
  "Every value below is stored on-chain and can be changed at any time without redeploying.\",\n          )}\n        </p>\n\n        <div className=\"grid md:grid-cols-2 gap-6\">\n          <div className=\"space-y-3\">\n            <h3 className=\"text-sm font-medium flex items-center gap-2\">\n              <Hash className=\"w-4 h-4\" /> {t(\"Serial Number Format",
  "Connect a BNB Chain compatible wallet to access {{BRAND}} features\",            )}          </p>          <ConnectButton.Custom>            {({              account,              chain,              openAccountModal,              openChainModal,              openConnectModal,              mounted,            }) => {              const connected = mounted && account && chain;              if (connected) {                return (                  <div className=\"mt-8 space-y-4\">                    <div className=\"p-4 rounded-lg border border-[rgba(79,169,124,0.40)] bg-[rgba(79,169,124,0.10)]\">                      <div className=\"flex items-center justify-center gap-2 text-[var(--success)]\">                        <CheckCircle2 className=\"w-5 h-5\" />                        <span className=\"font-semibold\">                          {t(\"Wallet Connected",
  "/api/admin/bulk-airdrop\", {    method: \"POST\",    body: JSON.stringify({ recipients }),  });}export async function getAdminLogs(): Promise<AdminLog[]> {  return request(\"/api/admin/logs",
  "We never store your private keys. All transactions are signed locally.\",          )}        </p>        <p className=\"mt-4 text-center text-xs\">          <Link            to=\"/dashboard\"            className=\"text-[var(--gold-lift)] hover:underline\"          >            {t(\"Skip to demo dashboard →",
  "Eligibility is verified manually off-chain, then included in a round's Merkle tree. You claim your allocation directly from the distributor contract — the project never batch-sends tokens, so timing and custody stay with you.\",              )}            </p>            {confirmed && (              <div className=\"mt-4 flex items-center gap-2 text-sm text-[var(--success)]\">                <CheckCircle2 className=\"w-4 h-4\" /> {t(\"Claim confirmed on-chain.",
  "/api/auth/verify\", {\n    method: \"POST\",\n    body: JSON.stringify({ address, signature }),\n  });\n}\n\n// ─── User Profile ───\n\nexport interface UserProfile {\n  id: string;\n  address: string;\n  display_name: string | null;\n  language: string;\n  notif_airdrop: boolean;\n  notif_staking: boolean;\n  notif_price: boolean;\n  created_at: string;\n  updated_at: string;\n}\n\nexport async function getProfile(): Promise<UserProfile> {\n  return request(\"/api/user/profile",
  "Manage your governance rights and ecosystem contributions.\",\n        )}\n      />\n\n      <div className=\"grid md:grid-cols-4 gap-4 mt-8\">\n        <Card className=\"md:col-span-1 bg-gradient-hero border-none\">\n          <div className=\"w-12 h-12 rounded-full bg-gradient-gold flex items-center justify-center text-gold-foreground font-bold mb-4\">\n            {data?.account?.role?.[0] || \"M\"}\n          </div>\n          <h2 className=\"font-semibold text-lg\">{t(\"DAO Role",
  "/api/transactions/record\", {    method: \"POST\",    body: JSON.stringify(tx),  });}// ─── Airdrop ───export interface AirdropStatus {  eligible: boolean;  webKeyBalance: string;  webKeyBalanceFormatted: string;  threshold: string;  thresholdFormatted: string;  rewardAmount: number;  /** Source of eligibility: \"BDFStaking\" (540-day lock) or legacy Block Label credit. */  source?: string;  /** Raw BDF staked (when using the staking contract). */  stakedWkeyDao2?: string;  /** Global stake order (drives airdrop round / early-bird tier). */  stakeSequence?: number;  /** Unix seconds when the 540-day lock ends. */  unlockTime?: number;  /** Seconds remaining on the 540-day lock. */  remainingLockSeconds?: string;}export async function getAirdropStatus(  address: string,): Promise<AirdropStatus> {  return request(`/api/airdrop/status/${address}`);}// ─── Airdrop Merkle claim (BDFAirdropDistributor) ───export interface AirdropProof {  round: number;  index: number;  account: string;  amount: string; // wei  proof: `0x${string}`[];}/** Fetch a wallet's Merkle claim for a round (404 if not included). */export async function getAirdropProof(  round: number,  address: string,): Promise<AirdropProof> {  return request(`/api/airdrop/proof/${round}/${address}`);}export interface AirdropRoundMeta {  round: number;  root: string;  count: number;}export async function getAirdropRound(round: number): Promise<AirdropRoundMeta> {  return request(`/api/airdrop/round/${round}`);}/** Admin: build a round's Merkle tree; returns the root to set on-chain. */export async function adminBuildRound(  round: number,  claims: { address: string; amount: string | number }[],): Promise<AirdropRoundMeta> {  return request(\"/api/admin/airdrop/build-round\", {    method: \"POST\",    body: JSON.stringify({ round, claims }),  });}// ─── Referrals ───export interface Referral {  id: string;  referrer_address: string;  referred_address: string;  join_date: string;  reward_amount: number;  status: string;}export interface ReferralData {  referrals: Referral[];  stats: {    totalReferrals: number;    totalRewards: number;    pendingRewards: number;    stakedBlockLabel?: string;  };}export async function getReferrals(address: string): Promise<ReferralData> {  return request(`/api/referral/${address}`);}// ─── DAO ───export interface DaoAccount {  address: string;  role: string;  tokens_held: number;  votes_cast: number;  rewards_earned: number;}export interface DaoProposal {  id: string;  proposal_id: string;  title: string;  status: string;  voted_at: string;}export interface DaoParticipation {  id: string;  type: string;  description: string;  date: string;  reward: number;}export interface DaoData {  account: DaoAccount;  proposals: DaoProposal[];  participation: DaoParticipation[];}export async function getDaoAccount(address: string): Promise<DaoData> {  return request(`/api/dao/account/${address}`);}export async function updateDaoAccount(  address: string,  updates: Partial<DaoAccount>,): Promise<DaoAccount> {  return request(`/api/dao/account/${address}`, {    method: \"PUT\",    body: JSON.stringify(updates),  });}// ─── Admin ───export async function adminAirdrop(  to: string,  amount: string,): Promise<{ txHash: string }> {  return request(\"/api/admin/airdrop\", {    method: \"POST\",    body: JSON.stringify({ to, amount }),  });}export interface AdminLog {  id: string;  action: string;  performed_by: string;  target_address: string;  amount: string;  tx_hash: string;  timestamp: string;}export interface UserEligibility {  address: string;  name: string | null;  webKeyBalance: number;  eligible: boolean;  rewardAmount: number;}export async function getEligibilityList(): Promise<UserEligibility[]> {  return request(\"/api/admin/users-eligibility",
  "{{BRAND}} ({{SYMBOL}}) is the native utility and governance token of the ecosystem, built on BNB Chain (BEP-20). The total supply is fixed at 10,000,000,000 {{SYMBOL}} with no minting capability, and 50% is allocated to the community through the Fan DAO and Creator DAO. Distribution is fully on-chain, transparent, and independently verifiable.\",          )}        </p>        <p className=\"mt-4 text-sm text-muted-foreground\">          {t(            \"Rather than an unconditional distribution, {{SYMBOL}} is claimed by verified ecosystem participants who meet corporate DAO and staking conditions. A rules-based buyback & burn — funded by real IP value growth — continuously reduces supply, and the team allocation is locked until the burn milestone is reached.\",          )}        </p>      </Card>      {/* Four pillars — About.pdf */}      <p className=\"mt-8 text-sm italic text-center text-muted-foreground max-w-xl mx-auto\">        {t(          \"“Building a healthy entertainment ecosystem where technology and art harmonize.”\",        )}      </p>      <div className=\"mt-6 grid sm:grid-cols-2 gap-5\">        {pillars.map((p) => (          <Card key={p.title}>            <div className=\"flex items-center gap-3\">              <div className=\"w-9 h-9 rounded-md bg-secondary flex items-center justify-center shrink-0\">                <p.icon className=\"w-4.5 h-4.5 text-gold-lift\" />              </div>              <h3 className=\"font-semibold text-base\">{t(p.title)}</h3>            </div>            <p className=\"mt-3 text-sm text-muted-foreground leading-relaxed\">              {t(p.body)}            </p>            <hr className=\"rule-gold my-4\" />            <ul className=\"space-y-1.5\">              {p.checklist.map((c) => (                <li key={c} className=\"flex items-center gap-2 text-sm\">                  <Check className=\"w-3.5 h-3.5 text-gold shrink-0\" />                  <span>{t(c)}</span>                </li>              ))}            </ul>          </Card>        ))}      </div>      {/* BSC ecosystem contribution — About.pdf */}      <Card className=\"mt-8\" ornament>        <div className=\"eyebrow mb-2\">{t(\"BSC Ecosystem Contribution",
  "Every value below is stored on-chain and can be changed at any time without redeploying.\",          )}        </p>        <div className=\"grid md:grid-cols-2 gap-6\">          <div className=\"space-y-3\">            <h3 className=\"text-sm font-medium flex items-center gap-2\">              <Hash className=\"w-4 h-4\" /> {t(\"Serial Number Format",
  "Ownership verified on-chain. You can browse and purchase IP wholesale products.\",\n                  )\n                : t(\n                    \"This wallet holds no Creator DAO Certificate. Certificates are issued by the Foundation once your order is confirmed.\",\n                  )}\n            </p>\n          </div>\n          {holds && (\n            <div className=\"text-right shrink-0\">\n              <div className=\"text-xs text-muted-foreground\">{t(\"Membership tier",
  "This console is limited to the operations wallet (issuance) and the Safe multisig (configuration).\",\n            )}\n          </p>\n          <div className=\"text-[11px] text-muted-foreground p-3 bg-secondary rounded-md text-left font-mono space-y-1\">\n            <div>Connected : {address || \"—\"}</div>\n            <div>Operations: {NFT_WALLETS.ops || \"not configured\"}</div>\n            <div>Safe      : {NFT_WALLETS.safe || \"not configured\"}</div>\n          </div>\n        </Card>\n      </Section>\n    );\n  }\n\n  const busy = isPending || confirming;\n  const exactSerialNum = Number(exactSerial);\n  const exactSerialValid =\n    Number.isInteger(exactSerialNum) && exactSerialNum >= 1 && exactSerialNum <= cap;\n\n  return (\n    <Section className=\"py-12\">\n      <div className=\"flex items-center justify-between flex-wrap gap-4 mb-8\">\n        <div>\n          <div className=\"eyebrow mb-1.5\">{t(\"Issuing Authority",
  "/api/user/profile\", {\n    method: \"PUT\",\n    body: JSON.stringify(updates),\n  });\n}\n\n// ─── Token ───\n\nexport interface TokenBalance {\n  address: string;\n  balance: string;\n  symbol: string;\n}\n\nexport async function getTokenBalance(address: string): Promise<TokenBalance> {\n  return request(`/api/token/balance/${address}`);\n}\n\nexport interface TokenStats {\n  totalSupply: string;\n  symbol: string;\n  chain: string;\n  contractAddress: string;\n}\n\nexport async function getTokenStats(): Promise<TokenStats> {\n  return request(\"/api/token/stats",
  "/api/admin/contract-status",
  "This console is limited to the operations wallet (issuance) and the Safe multisig (configuration).\",            )}          </p>          <div className=\"text-[11px] text-muted-foreground p-3 bg-secondary rounded-md text-left font-mono space-y-1\">            <div>Connected : {address || \"—\"}</div>            <div>Operations: {NFT_WALLETS.ops || \"not configured\"}</div>            <div>Safe      : {NFT_WALLETS.safe || \"not configured\"}</div>          </div>        </Card>      </Section>    );  }  const busy = isPending || confirming;  const exactSerialNum = Number(exactSerial);  const exactSerialValid =    Number.isInteger(exactSerialNum) && exactSerialNum >= 1 && exactSerialNum <= cap;  return (    <Section className=\"py-12\">      <div className=\"flex items-center justify-between flex-wrap gap-4 mb-8\">        <div>          <div className=\"eyebrow mb-1.5\">{t(\"Issuing Authority",
  "Manage your governance rights and ecosystem contributions.\",        )}      />      <div className=\"grid md:grid-cols-4 gap-4 mt-8\">        <Card className=\"md:col-span-1 bg-gradient-hero border-none\">          <div className=\"w-12 h-12 rounded-full bg-gradient-gold flex items-center justify-center text-gold-foreground font-bold mb-4\">            {data?.account?.role?.[0] || \"M\"}          </div>          <h2 className=\"font-semibold text-lg\">{t(\"DAO Role",
  "Invite wallets to the {{BRAND}} ecosystem and earn rewards.\",        )}      />      <Card>        <label className=\"text-sm text-muted-foreground\">          {t(\"Your Referral Link",
  "Please switch your wallet to BNB Smart Chain to interact with {{BRAND}}.\",\n            )}\n          </p>\n          <GoldButton\n            onClick={() =>\n              switchChain && switchChain({ chainId: REQUIRED_CHAIN_ID })\n            }\n            disabled={!switchChain}\n            className=\"w-full justify-center\"\n          >\n            {t(\"Switch Network",
  "Pausing halts all issuance AND all holder transfers. Use only for a live security incident.\",\n              )}\n            </p>\n            <OutlineButton\n              className=\"w-full px-4 py-2\"\n              disabled={!isAdmin || busy}\n              onClick={() => {\n                if (!confirm(t(\"Confirm pause state change for the whole collection?",
  "@tanstack/react-start/server-entry",
  "Block Label collaborates with diverse partners and ecosystem projects to create synergy between content IP and blockchain technology.\",\n          )}\n        </p>\n      </div>\n      <div className=\"mt-6 grid sm:grid-cols-2 gap-5\">\n        {partners.map((p) => (\n          <Card key={p.name} className=\"text-center\">\n            <div className=\"font-engraved text-lg font-medium\">{p.name}</div>\n            <p className=\"mt-2 text-sm text-muted-foreground\">{t(p.body)}</p>\n          </Card>\n        ))}\n      </div>\n\n      {/* Key Facts */}\n      <h2 className=\"mt-12 mb-4 text-xl font-semibold\">{t(\"Key Facts",
  "{{SYMBOL}} is fixed-supply (no mint). Distributions are sent from the treasury or claimed via the airdrop distributor.\",\n                )}\n              </div>\n              <div className=\"pt-4 border-t border-border flex items-center justify-between\">\n                <div>\n                  <div className=\"text-sm\">{t(\"Pause Contract",
  "The Foundation issues the next certificate serial directly to your BSC wallet — no action needed from you.\",\n              )}\n            </li>\n            <li>{t(\"Your certificate then appears on this page automatically.",
  "Use when the buyer must receive the number printed on their physical certificate.\",\n              )}\n            </p>\n            <div className=\"grid grid-cols-3 gap-2\">\n              <div className=\"col-span-2\">\n                <Field\n                  label={t(\"Buyer BSC wallet",
  "/api/auth/verify\", {    method: \"POST\",    body: JSON.stringify({ address, signature }),  });}// ─── User Profile ───export interface UserProfile {  id: string;  address: string;  display_name: string | null;  language: string;  notif_airdrop: boolean;  notif_staking: boolean;  notif_price: boolean;  created_at: string;  updated_at: string;}export async function getProfile(): Promise<UserProfile> {  return request(\"/api/user/profile",
  "Lowering the supply cap is permanent — it can never be raised again. Continue?\",                    ),                  )                )                  return;                send(t(\"Supply cap reduction",
  "Connect a BNB Chain compatible wallet to access {{BRAND}} features\",\n            )}\n          </p>\n\n          <ConnectButton.Custom>\n            {({\n              account,\n              chain,\n              openAccountModal,\n              openChainModal,\n              openConnectModal,\n              mounted,\n            }) => {\n              const connected = mounted && account && chain;\n\n              if (connected) {\n                return (\n                  <div className=\"mt-8 space-y-4\">\n                    <div className=\"p-4 rounded-lg border border-[rgba(79,169,124,0.40)] bg-[rgba(79,169,124,0.10)]\">\n                      <div className=\"flex items-center justify-center gap-2 text-[var(--success)]\">\n                        <CheckCircle2 className=\"w-5 h-5\" />\n                        <span className=\"font-semibold\">\n                          {t(\"Wallet Connected",
  "Block Label collaborates with diverse partners and ecosystem projects to create synergy between content IP and blockchain technology.\",          )}        </p>      </div>      <div className=\"mt-6 grid sm:grid-cols-2 gap-5\">        {partners.map((p) => (          <Card key={p.name} className=\"text-center\">            <div className=\"font-engraved text-lg font-medium\">{p.name}</div>            <p className=\"mt-2 text-sm text-muted-foreground\">{t(p.body)}</p>          </Card>        ))}      </div>      {/* Key Facts */}      <h2 className=\"mt-12 mb-4 text-xl font-semibold\">{t(\"Key Facts",
  "The Foundation issues the next certificate serial directly to your BSC wallet — no action needed from you.\",              )}            </li>            <li>{t(\"Your certificate then appears on this page automatically.",
  "{{SYMBOL}} is fixed-supply (no mint). Distributions are sent from the treasury or claimed via the airdrop distributor.\",                )}              </div>              <div className=\"pt-4 border-t border-border flex items-center justify-between\">                <div>                  <div className=\"text-sm\">{t(\"Pause Contract",
  "The BDF token is a BEP-20 asset issued on the BSC (Binance Smart Chain) network. Block Label leverages BSC's high throughput, low fees, and proven security to bring entertainment IP and the fan economy on-chain — contributing to the expansion of real-world use cases within the BSC ecosystem.\",          )}        </p>        <div className=\"mt-6 grid sm:grid-cols-3 gap-4\">          {bscPoints.map((b) => (            <div key={b.title} className=\"p-4 rounded-lg tile\">              <b.icon className=\"w-4.5 h-4.5 text-gold-lift\" />              <div className=\"mt-2.5 text-sm font-semibold\">{t(b.title)}</div>              <p className=\"mt-1.5 text-xs text-muted-foreground leading-relaxed\">                {t(b.body)}              </p>            </div>          ))}        </div>        <div className=\"mt-6 flex gap-2.5 text-xs text-muted-foreground p-3 rounded-md bg-secondary\">          <Info className=\"w-3.5 h-3.5 shrink-0 mt-0.5\" />          <p>            {t(              \"This is an independent project built on the BSC (Binance Smart Chain) network. While we currently have no capital or equity ties with Binance or its affiliates, we are actively pursuing strategic collaborations and partnerships with projects across the BSC ecosystem going forward.\",            )}          </p>        </div>      </Card>      {/* Partners & Ecosystem — About.pdf */}      <div className=\"mt-12 text-center\">        <div className=\"eyebrow\">{t(\"Partners & Ecosystem",
  "\\n",
  "Eligibility is verified manually off-chain, then included in a round's Merkle tree. You claim your allocation directly from the distributor contract — the project never batch-sends tokens, so timing and custody stay with you.\",\n              )}\n            </p>\n            {confirmed && (\n              <div className=\"mt-4 flex items-center gap-2 text-sm text-[var(--success)]\">\n                <CheckCircle2 className=\"w-4 h-4\" /> {t(\"Claim confirmed on-chain."
]